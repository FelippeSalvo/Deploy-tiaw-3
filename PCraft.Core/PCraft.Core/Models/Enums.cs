namespace PCraft.Core.Models
{
    public enum CpuSocket { LGA1700, LGA1200, AM5, AM4 }
    public enum RamType { DDR4, DDR5 }
    public enum GpuInterface { PCIe30, PCIe40, PCIe50 }
    public enum PsuCertification { Bronze, Silver, Gold, Platinum }
}
